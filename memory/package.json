{
  "name": "memory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate": "node migrate.mjs",
    "verify": "node verify.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "vitest": "^2.1.5"
  }
}
