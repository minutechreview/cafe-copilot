# Memory

Placeholder. The CockroachDB persistent-memory schema (conversations, business context
notes, drafts, and embedded/vector-indexed documents) lands in Phase C2. See
`docs/CONTRACTS.md` and the design contract for the intended shape.
